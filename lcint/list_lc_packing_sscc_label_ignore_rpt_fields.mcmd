<command>
<name>list lc packing sscc label ignore rpt fields</name>
<description>list lc packing sscc label ignore rpt fields</description>
<type>Local Syntax</type>
<local-syntax>
<![CDATA[

validate lc packing sscc label fields
|
/* No existing MOCA list command, use select statement instead.
 * Retrieve data from some tables directly since there is no view for these tables.
 * Use raw because MOCA logic requires literal substitution.
 * We do not place this complex SQL in a separate command because the business logic cannot separate this complex SQL from field validation logic.
 */
[select @result_string:raw
   from dual]


]]>
</local-syntax>

<documentation>
<remarks>
<![CDATA[
    K+N reference for change: SWIFTLEX-58487
    @author Reko/Lucas Lu
    @version 1.0.0
    @since 2025-10-22
    <p>
    This command is used to list lc packing sscc label ignore rpt fields.
    </p>
]]>
</remarks>
</documentation>
</command>